import {StyleSheet} from 'react-native';

const styles = () => {
  return StyleSheet.create({
    searchBar: {
      elevation: 2,
      backgroundColor: '#fff',
      borderRadius: 30,
      borderWidth: 1,
      borderColor: '#fff',
    },
  });
};

export default styles;
