import {StyleSheet} from 'react-native';

const styles = () => {
  return StyleSheet.create({
    searchBar: {},
    textAdress: {
      color: '#3F3D56',
    },
    IconsText: {
      color: '#3F3D56',
      textAlign: 'center',
    },
    iconAdress: {
      width: 20,
      height: 20,
    },
  });
};

export default styles;
