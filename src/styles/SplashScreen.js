import {StyleSheet} from 'react-native';

const styles = () => {
  return StyleSheet.create({
    button: {
      width: 140,
    },
    apolloImage: {
      width: 410,
      height: 100,
      marginBottom: 30,
      marginTop: 60,
    },
  });
};

export default styles;
