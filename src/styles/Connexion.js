import {StyleSheet} from 'react-native';

const styles = () => {
  return StyleSheet.create({
    bienvenu: {
      textAlign: 'center',
      letterSpacing: 0.1,
    },
    loginButton: {
      width: 220,
      backgroundColor: '#FFF',
    },
    input: {
      width: 330,
    },
    image: {
      width: 230,
      height: 70,
      marginTop: 15,
    },
  });
};

export default styles;
