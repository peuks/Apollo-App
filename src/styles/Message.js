import {StyleSheet} from 'react-native';

const styles = () => {
  return StyleSheet.create({
    searchBar: {
      borderRadius: 50,
    },
    text: {
      textAlign: 'center',
    },
  });
};

export default styles;
